name: CI

on:
  pull_request:
    branches: [dev, review, qa, live]

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ---------------------------------------------------------------
      # TODO(stack): PLACEHOLDER — replace with real commands.
      # At setup time this repo contained no dependency manifest
      # (no package.json, requirements.txt, pyproject.toml, pom.xml,
      # go.mod), so the install/test commands could not be detected.
      # ---------------------------------------------------------------
      - name: Install dependencies
        run: echo "TODO: replace with real install command (e.g. npm ci)"

      - name: Run tests
        run: echo "TODO: replace with real test command (e.g. npm test)"
