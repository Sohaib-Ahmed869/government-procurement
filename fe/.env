VITE_API_BASE_URL=https://government-procurement.onrender.com/api
#VITE_API_BASE_URL=http://localhost:5000/api
VITE_GTM_ID=

# B7 — Find a Bid Writer. off | preview | live.
# Local and staging run `preview`: the page works but stays out of the nav
# and marks itself noindex. Production must be `off` until go-live.
# See docs/GO-LIVE-BID-WRITERS.md.
VITE_FEATURE_BID_WRITERS=off
